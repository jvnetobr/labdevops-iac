- hosts: 'terraform-ansible'
  tasks:
  - name: 'instalando python3 e virtualenv'
    apt:
      pkg:
      - python3
      - virtualenv
      update_cache: yes 
    become: yes
  - name: 'instalando dependências do python3'
    pip:
      virtualenv: /home/ubuntu/tcc/venv
      name:
      - django
      - djangorestframework